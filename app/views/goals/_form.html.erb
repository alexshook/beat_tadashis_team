<%= form_for @goal do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :desc %>
  <%= f.text_area :desc %>

  <%= f.hidden_field :company_id, value: @company.id %>

  <%= f.submit %>

<% end %>
